#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int n, m, p;
    cin>>n>>m>>p;
    int k=p/n;
    int l=k*m;
    cout<<l;
    return 0;
}